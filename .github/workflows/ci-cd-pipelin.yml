#################################################################################
# Copyright (c) 2025 ARENA2036 e.V.
#
# SPDX-License-Identifier: Apache-2.0
#################################################################################

name: SDE CI-CD Pipeline

# Deployment rules:
#
# Developer Preview:
#   feature/**, bugfix/**, hotfix/**, chore/**, docs/**
#   Automatic on push
#   Cluster: arenax-ap6
#   Values: values-dev.yaml
#
# Shared Development:
#   develop
#   Manual with environment=dev-ap6
#   Cluster: arenax-ap6
#   Values: values-dev.yaml
#
# Staging:
#   develop
#   Manual with environment=staging
#   Cluster: arenax-staging
#   Values: values-staging.yaml
#
# Production:
#   release/**
#   Manual with environment=prod
#   Cluster: arenax-prod
#   Values: values-prod.yaml
#
# This repository does not build Docker images.
# Images must already exist in Harbor.

on:
  push:
    branches:
      - "feature/**"
      - "bugfix/**"
      - "hotfix/**"
      - "chore/**"
      - "docs/**"
      - ci-cd-branch

  workflow_dispatch:
    inputs:
      environment:
        description: "Select the target environment for deployment"
        required: true
        default: dev-ap6
        type: choice
        options:
          - dev-ap6
          - staging
          - prod

      image_tag:
        description: "Optional existing image tag for backend and frontend"
        required: false
        type: string

      backend_image_tag:
        description: "Optional existing backend image tag; overrides the common image tag"
        required: false
        type: string

      frontend_image_tag:
        description: "Optional existing frontend image tag; overrides the common image tag"
        required: false
        type: string

permissions:
  contents: write
  actions: read
  security-events: write
  id-token: write

  # Required by the current reusable TruffleHog workflow.
  issues: write

concurrency:
  group: sde-deployment-${{ github.ref }}
  cancel-in-progress: false

env:
  CHART_PATH: charts/simpledataexchanger

  DEV_VALUES_FILE: charts/simpledataexchanger/values-dev.yaml
  STAGING_VALUES_FILE: charts/simpledataexchanger/values-staging.yaml
  PROD_VALUES_FILE: charts/simpledataexchanger/values-prod.yaml

  BACKEND_IMAGE_REPOSITORY: harbor-hub-shared.arena.3ascloud.de/arena2036/managed-simple-data-exchanger-backend
  FRONTEND_IMAGE_REPOSITORY: harbor-hub-shared.arena.3ascloud.de/arena2036/managed-simple-data-exchanger-frontend

  # Kubeconfig for the cluster hosting Central Argo CD.
  CENTRAL_ARGOCD_VAULT_PATH: staging/infra/cluster-prod

  VAULT_VERSION: "1.21.2"

jobs:
  # =================================================================
  # Scan the Helm repository for secrets.
  # trufflehog.yml must support workflow_call.
  # =================================================================
  trufflehog:
    name: Secret Scan
    uses: ./.github/workflows/trufflehog.yml
    secrets: inherit

  # =================================================================
  # Resolve deployment environment, values file and image tags.
  # =================================================================
  resolve-deployment:
    name: Resolve Deployment
    needs: trufflehog
    runs-on: ubuntu-latest

    outputs:
      deployment_type: ${{ steps.resolve.outputs.deployment_type }}
      values_file: ${{ steps.resolve.outputs.values_file }}
      template_file: ${{ steps.resolve.outputs.template_file }}

      backend_tag: ${{ steps.resolve.outputs.backend_tag }}
      frontend_tag: ${{ steps.resolve.outputs.frontend_tag }}

      target_branch: ${{ steps.resolve.outputs.target_branch }}
      application_name: ${{ steps.resolve.outputs.application_name }}
      release_name: ${{ steps.resolve.outputs.release_name }}
      namespace: ${{ steps.resolve.outputs.namespace }}
      safe_branch: ${{ steps.resolve.outputs.safe_branch }}

    steps:
      - name: Resolve deployment configuration
        id: resolve
        shell: bash
        run: |
          set -euo pipefail

          EVENT_NAME="${{ github.event_name }}"
          BRANCH_NAME="${{ github.ref_name }}"
          TARGET_ENVIRONMENT="${{ github.event.inputs.environment }}"

          COMMON_TAG="${{ github.event.inputs.image_tag }}"
          BACKEND_TAG="${{ github.event.inputs.backend_image_tag }}"
          FRONTEND_TAG="${{ github.event.inputs.frontend_image_tag }}"

          # Component tags override the common image tag.
          if [[ -z "$BACKEND_TAG" ]]; then
            BACKEND_TAG="$COMMON_TAG"
          fi

          if [[ -z "$FRONTEND_TAG" ]]; then
            FRONTEND_TAG="$COMMON_TAG"
          fi

          # Docker tag:
          # feature/new-ui -> feature-new-ui
          BRANCH_TAG="$(printf '%s' "$BRANCH_NAME" \
            | tr '[:upper:]' '[:lower:]' \
            | sed 's#[^a-z0-9._-]#-#g' \
            | sed 's/--*/-/g' \
            | sed 's/^-//' \
            | sed 's/-$//' \
            | cut -c1-100)"

          # Kubernetes-safe preview identifier.
          DNS_BRANCH="$(printf '%s' "$BRANCH_NAME" \
            | tr '[:upper:]' '[:lower:]' \
            | sed 's#[^a-z0-9-]#-#g' \
            | sed 's/--*/-/g' \
            | sed 's/^-//' \
            | sed 's/-$//' \
            | cut -c1-35)"

          if [[ -z "$BRANCH_TAG" || -z "$DNS_BRANCH" ]]; then
            echo "Could not generate safe branch identifiers"
            exit 1
          fi

          BRANCH_HASH="$(printf '%s' "$BRANCH_NAME" \
            | sha256sum \
            | cut -c1-7)"

          SAFE_BRANCH="${DNS_BRANCH}-${BRANCH_HASH}"

          # ---------------------------------------------------------
          # Automatic developer preview.
          # ---------------------------------------------------------
          if [[ "$EVENT_NAME" == "push" ]]; then
            if [[ "$BRANCH_NAME" != "ci-cd-branch" &&
                  "$BRANCH_NAME" != feature/* &&
                  "$BRANCH_NAME" != bugfix/* &&
                  "$BRANCH_NAME" != hotfix/* &&
                  "$BRANCH_NAME" != chore/* &&
                  "$BRANCH_NAME" != docs/* ]]; then

              echo "Automatic preview is not allowed from: $BRANCH_NAME"
              exit 1
            fi

            DEPLOYMENT_TYPE="preview"
            VALUES_FILE="$DEV_VALUES_FILE"
            TEMPLATE_FILE=".github/argocd/sde-preview-app.yaml"

            # Matching tags must already exist in both source repos.
            BACKEND_TAG="$BRANCH_TAG"
            FRONTEND_TAG="$BRANCH_TAG"

            TARGET_BRANCH="$BRANCH_NAME"
            APPLICATION_NAME="sde-${SAFE_BRANCH}"
            RELEASE_NAME="sde-${SAFE_BRANCH}"
            NAMESPACE="sde-${SAFE_BRANCH}"

          # ---------------------------------------------------------
          # Manual shared development deployment.
          # ---------------------------------------------------------
          elif [[ "$EVENT_NAME" == "workflow_dispatch" &&
                  "$TARGET_ENVIRONMENT" == "dev-ap6" &&
                  "$BRANCH_NAME" == "develop" ]]; then

            DEPLOYMENT_TYPE="shared-dev"
            VALUES_FILE="$DEV_VALUES_FILE"
            TEMPLATE_FILE=".github/argocd/sde-dev-app.yaml"

            if [[ -z "$BACKEND_TAG" ]]; then
              BACKEND_TAG="develop"
            fi

            if [[ -z "$FRONTEND_TAG" ]]; then
              FRONTEND_TAG="develop"
            fi

            TARGET_BRANCH="develop"
            APPLICATION_NAME="sde-application-ap6"
            RELEASE_NAME="sde"
            NAMESPACE="simple-data-exchanger"

          # ---------------------------------------------------------
          # Manual staging deployment.
          # ---------------------------------------------------------
          elif [[ "$EVENT_NAME" == "workflow_dispatch" &&
                  "$TARGET_ENVIRONMENT" == "staging" &&
                  "$BRANCH_NAME" == "develop" ]]; then

            DEPLOYMENT_TYPE="staging"
            VALUES_FILE="$STAGING_VALUES_FILE"
            TEMPLATE_FILE=".github/argocd/sde-staging-app.yaml"

            if [[ -z "$BACKEND_TAG" ]]; then
              BACKEND_TAG="develop"
            fi

            if [[ -z "$FRONTEND_TAG" ]]; then
              FRONTEND_TAG="develop"
            fi

            TARGET_BRANCH="develop"
            APPLICATION_NAME="sde-application-staging"
            RELEASE_NAME="sde"
            NAMESPACE="simple-data-exchanger"

          # ---------------------------------------------------------
          # Manual production deployment.
          # ---------------------------------------------------------
          elif [[ "$EVENT_NAME" == "workflow_dispatch" &&
                  "$TARGET_ENVIRONMENT" == "prod" &&
                  "$BRANCH_NAME" == release/* ]]; then

            DEPLOYMENT_TYPE="production"
            VALUES_FILE="$PROD_VALUES_FILE"
            TEMPLATE_FILE=".github/argocd/sde-prod-app.yaml"

            # Do not use the Helm repository SHA as an image tag.
            if [[ -z "$BACKEND_TAG" || -z "$FRONTEND_TAG" ]]; then
              echo "Production requires existing image tags"
              echo
              echo "Provide a common image_tag or both:"
              echo "  backend_image_tag"
              echo "  frontend_image_tag"
              exit 1
            fi

            TARGET_BRANCH="$BRANCH_NAME"
            APPLICATION_NAME="sde-application-prod"
            RELEASE_NAME="sde"
            NAMESPACE="simple-data-exchanger"

          else
            echo "Invalid branch/environment combination"
            echo
            echo "Allowed deployments:"
            echo "  push feature/**, bugfix/**, hotfix/**, chore/**, docs/**"
            echo "  develop + dev-ap6"
            echo "  develop + staging"
            echo "  release/** + prod"
            exit 1
          fi

          {
            echo "deployment_type=$DEPLOYMENT_TYPE"
            echo "values_file=$VALUES_FILE"
            echo "template_file=$TEMPLATE_FILE"

            echo "backend_tag=$BACKEND_TAG"
            echo "frontend_tag=$FRONTEND_TAG"

            echo "target_branch=$TARGET_BRANCH"
            echo "application_name=$APPLICATION_NAME"
            echo "release_name=$RELEASE_NAME"
            echo "namespace=$NAMESPACE"
            echo "safe_branch=$SAFE_BRANCH"
          } >> "$GITHUB_OUTPUT"

          echo "Deployment: $DEPLOYMENT_TYPE"
          echo "Values file: $VALUES_FILE"
          echo "Backend: $BACKEND_IMAGE_REPOSITORY:$BACKEND_TAG"
          echo "Frontend: $FRONTEND_IMAGE_REPOSITORY:$FRONTEND_TAG"

  # =================================================================
  # Verify the selected backend and frontend images exist in Harbor.
  # No Docker image is built here.
  # =================================================================
  verify-images:
    name: Verify Existing Harbor Images
    needs: resolve-deployment
    runs-on: ubuntu-latest

    steps:
      - name: Install skopeo
        shell: bash
        run: |
          set -euo pipefail

          sudo apt-get update
          sudo apt-get install -y skopeo

          skopeo --version

      # Retry because frontend/backend builds can complete at
      # slightly different times.
      - name: Verify backend and frontend images
        env:
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          BACKEND_IMAGE="${BACKEND_IMAGE_REPOSITORY}:${BACKEND_TAG}"
          FRONTEND_IMAGE="${FRONTEND_IMAGE_REPOSITORY}:${FRONTEND_TAG}"

          for ATTEMPT in {1..20}; do
            BACKEND_FOUND=false
            FRONTEND_FOUND=false

            if skopeo inspect \
              --creds "${{ secrets.HBR_USERNAME }}:${{ secrets.HBR_PASSWORD }}" \
              "docker://${BACKEND_IMAGE}" \
              >/dev/null 2>&1; then

              BACKEND_FOUND=true
            fi

            if skopeo inspect \
              --creds "${{ secrets.HBR_USERNAME }}:${{ secrets.HBR_PASSWORD }}" \
              "docker://${FRONTEND_IMAGE}" \
              >/dev/null 2>&1; then

              FRONTEND_FOUND=true
            fi

            echo "Attempt ${ATTEMPT}/20"
            echo "Backend found: $BACKEND_FOUND"
            echo "Frontend found: $FRONTEND_FOUND"

            if [[ "$BACKEND_FOUND" == true &&
                  "$FRONTEND_FOUND" == true ]]; then

              echo "Both Harbor images exist"
              exit 0
            fi

            sleep 30
          done

          echo "Required images were not found"
          echo "Backend: $BACKEND_IMAGE"
          echo "Frontend: $FRONTEND_IMAGE"
          exit 1

      - name: Write image summary
        shell: bash
        run: |
          {
            echo "## Verified SDE images"
            echo
            echo "| Component | Image |"
            echo "|---|---|"
            echo "| Backend | \`${BACKEND_IMAGE_REPOSITORY}:${{ needs.resolve-deployment.outputs.backend_tag }}\` |"
            echo "| Frontend | \`${FRONTEND_IMAGE_REPOSITORY}:${{ needs.resolve-deployment.outputs.frontend_tag }}\` |"
          } >> "$GITHUB_STEP_SUMMARY"

  # =================================================================
  # Update values-dev.yaml, values-staging.yaml or values-prod.yaml.
  # =================================================================
  update-image-tag:
    name: Update Helm Image Tags for Deployment
    needs:
      - resolve-deployment
      - verify-images

    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout deployment branch
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-deployment.outputs.target_branch }}
          fetch-depth: 0

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Install yq
        shell: bash
        run: |
          set -euo pipefail

          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

          sudo chmod +x /usr/local/bin/yq
          yq --version

      - name: Update backend and frontend image tags
        env:
          VALUES_FILE: ${{ needs.resolve-deployment.outputs.values_file }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          if [[ ! -f "$VALUES_FILE" ]]; then
            echo "Values file not found: $VALUES_FILE"
            exit 1
          fi

          yq -i \
            '.backend.image.repository = strenv(BACKEND_IMAGE_REPOSITORY)' \
            "$VALUES_FILE"

          yq -i \
            '.backend.image.tag = strenv(BACKEND_TAG)' \
            "$VALUES_FILE"

          yq -i \
            '.frontend.image.repository = strenv(FRONTEND_IMAGE_REPOSITORY)' \
            "$VALUES_FILE"

          yq -i \
            '.frontend.image.tag = strenv(FRONTEND_TAG)' \
            "$VALUES_FILE"

          echo "Updated Helm values:"
          git diff -- "$VALUES_FILE"

      - name: Validate Helm values
        env:
          VALUES_FILE: ${{ needs.resolve-deployment.outputs.values_file }}
          TARGET_NAMESPACE: ${{ needs.resolve-deployment.outputs.namespace }}

        shell: bash
        run: |
          set -euo pipefail

          helm dependency update "$CHART_PATH"

          helm lint \
            "$CHART_PATH" \
            --with-subcharts \
            --values "$VALUES_FILE"

          helm template sde \
            "$CHART_PATH" \
            --namespace "$TARGET_NAMESPACE" \
            --values "$VALUES_FILE" \
            > /tmp/sde-rendered.yaml

          test -s /tmp/sde-rendered.yaml

      - name: Commit updated Helm values
        env:
          VALUES_FILE: ${{ needs.resolve-deployment.outputs.values_file }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}
          TARGET_BRANCH: ${{ needs.resolve-deployment.outputs.target_branch }}

        shell: bash
        run: |
          set -euo pipefail

          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add -f "$VALUES_FILE"

          if git diff --cached --quiet; then
            echo "No Helm values changes to commit"
            exit 0
          fi

          git commit \
            -m "chore: update SDE image tags [skip ci]"

          git pull --rebase origin "$TARGET_BRANCH"
          git push origin "HEAD:${TARGET_BRANCH}"

  # =================================================================
  # Developer Preview — AP6.
  # =================================================================
  deploy-preview:
    name: Deploy Developer Preview
    needs:
      - resolve-deployment
      - update-image-tag

    if: |
      needs.resolve-deployment.outputs.deployment_type == 'preview'

    runs-on: ubuntu-latest
    environment: sde-preview

    steps:
      - name: Checkout preview branch
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-deployment.outputs.target_branch }}
          fetch-depth: 0

      - name: Install tools
        shell: bash
        run: |
          set -euo pipefail

          KUBECTL_VERSION="$(curl -L -s https://dl.k8s.io/release/stable.txt)"

          curl -LO -s \
            "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"

          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl

          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

          sudo chmod +x /usr/local/bin/yq

          curl -sSL \
            "https://releases.hashicorp.com/vault/${VAULT_VERSION}/vault_${VAULT_VERSION}_linux_amd64.zip" \
            -o vault.zip

          unzip -q vault.zip
          chmod +x vault
          sudo mv vault /usr/local/bin/vault

      - name: Connect to Central Argo CD cluster
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/.kube"

          export VAULT_ADDR="${{ secrets.VAULT_ADDR_PROD }}"
          export VAULT_TOKEN="${{ secrets.VAULT_TOKEN_PROD }}"

          vault kv get \
            -field=config \
            "$CENTRAL_ARGOCD_VAULT_PATH" \
            > "$HOME/.kube/config"

          chmod 600 "$HOME/.kube/config"

          CONTEXT="$(yq e '.current-context' "$HOME/.kube/config")"

          if [[ -z "$CONTEXT" || "$CONTEXT" == "null" ]]; then
            echo "Central Argo CD context was not found"
            exit 1
          fi

          kubectl --context="$CONTEXT" cluster-info
          echo "ARGOCD_KUBE_CONTEXT=$CONTEXT" >> "$GITHUB_ENV"

      - name: Generate preview Argo CD Application
        env:
          TEMPLATE_FILE: ${{ needs.resolve-deployment.outputs.template_file }}
          APPLICATION_NAME: ${{ needs.resolve-deployment.outputs.application_name }}
          RELEASE_NAME: ${{ needs.resolve-deployment.outputs.release_name }}
          TARGET_BRANCH: ${{ needs.resolve-deployment.outputs.target_branch }}
          TARGET_NAMESPACE: ${{ needs.resolve-deployment.outputs.namespace }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          GENERATED="/tmp/sde-preview-app.yaml"

          cp "$TEMPLATE_FILE" "$GENERATED"

          sed -i "s|REPLACE_APPLICATION|${APPLICATION_NAME}|g" "$GENERATED"
          sed -i "s|REPLACE_BRANCH_NAME|${TARGET_BRANCH}|g" "$GENERATED"
          sed -i "s|REPLACE_RELEASE_NAME|${RELEASE_NAME}|g" "$GENERATED"
          sed -i "s|REPLACE_BACKEND_TAG|${BACKEND_TAG}|g" "$GENERATED"
          sed -i "s|REPLACE_FRONTEND_TAG|${FRONTEND_TAG}|g" "$GENERATED"
          sed -i "s|REPLACE_NAMESPACE|${TARGET_NAMESPACE}|g" "$GENERATED"

          if grep -q 'REPLACE_' "$GENERATED"; then
            echo "Unresolved placeholders:"
            grep 'REPLACE_' "$GENERATED"
            exit 1
          fi

          kubectl apply \
            --dry-run=client \
            --validate=false \
            -f "$GENERATED"

          cat "$GENERATED"

      - name: Apply preview Application
        shell: bash
        run: |
          set -euo pipefail

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            apply \
            -f /tmp/sde-preview-app.yaml

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            get application \
            "${{ needs.resolve-deployment.outputs.application_name }}" \
            -n argocd

  # =================================================================
  # Shared Development — AP6.
  # =================================================================
  deploy-shared-dev:
    name: Deploy Shared Development
    needs:
      - resolve-deployment
      - update-image-tag

    if: |
      needs.resolve-deployment.outputs.deployment_type == 'shared-dev'

    runs-on: ubuntu-latest
    environment: sde-dev-ap6

    steps:
      - name: Checkout develop
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Install tools
        shell: bash
        run: |
          set -euo pipefail

          KUBECTL_VERSION="$(curl -L -s https://dl.k8s.io/release/stable.txt)"

          curl -LO -s \
            "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"

          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl

          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

          sudo chmod +x /usr/local/bin/yq

          curl -sSL \
            "https://releases.hashicorp.com/vault/${VAULT_VERSION}/vault_${VAULT_VERSION}_linux_amd64.zip" \
            -o vault.zip

          unzip -q vault.zip
          chmod +x vault
          sudo mv vault /usr/local/bin/vault

      - name: Connect to Central Argo CD cluster
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/.kube"

          export VAULT_ADDR="${{ secrets.VAULT_ADDR_PROD }}"
          export VAULT_TOKEN="${{ secrets.VAULT_TOKEN_PROD }}"

          vault kv get \
            -field=config \
            "$CENTRAL_ARGOCD_VAULT_PATH" \
            > "$HOME/.kube/config"

          chmod 600 "$HOME/.kube/config"

          CONTEXT="$(yq e '.current-context' "$HOME/.kube/config")"

          if [[ -z "$CONTEXT" || "$CONTEXT" == "null" ]]; then
            echo "Central Argo CD context was not found"
            exit 1
          fi

          kubectl --context="$CONTEXT" cluster-info
          echo "ARGOCD_KUBE_CONTEXT=$CONTEXT" >> "$GITHUB_ENV"

      - name: Generate Shared Development Application
        env:
          TEMPLATE_FILE: ${{ needs.resolve-deployment.outputs.template_file }}
          APPLICATION_NAME: ${{ needs.resolve-deployment.outputs.application_name }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          GENERATED="/tmp/sde-dev-app.yaml"

          cp "$TEMPLATE_FILE" "$GENERATED"

          sed -i "s|REPLACE_APPLICATION|${APPLICATION_NAME}|g" "$GENERATED"
          sed -i "s|REPLACE_BACKEND_TAG|${BACKEND_TAG}|g" "$GENERATED"
          sed -i "s|REPLACE_FRONTEND_TAG|${FRONTEND_TAG}|g" "$GENERATED"

          if grep -q 'REPLACE_' "$GENERATED"; then
            echo "Unresolved placeholders:"
            grep 'REPLACE_' "$GENERATED"
            exit 1
          fi

      - name: Apply Shared Development Application
        shell: bash
        run: |
          set -euo pipefail

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            apply \
            -f /tmp/sde-dev-app.yaml

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            get application sde-application-ap6 \
            -n argocd

  # =================================================================
  # Staging — arenax-staging.
  # =================================================================
  deploy-staging:
    name: Deploy Staging
    needs:
      - resolve-deployment
      - update-image-tag

    if: |
      needs.resolve-deployment.outputs.deployment_type == 'staging'

    runs-on: ubuntu-latest
    environment: sde-staging

    steps:
      - name: Checkout develop
        uses: actions/checkout@v4
        with:
          ref: develop
          fetch-depth: 0

      - name: Install tools
        shell: bash
        run: |
          set -euo pipefail

          KUBECTL_VERSION="$(curl -L -s https://dl.k8s.io/release/stable.txt)"

          curl -LO -s \
            "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"

          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl

          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

          sudo chmod +x /usr/local/bin/yq

          curl -sSL \
            "https://releases.hashicorp.com/vault/${VAULT_VERSION}/vault_${VAULT_VERSION}_linux_amd64.zip" \
            -o vault.zip

          unzip -q vault.zip
          chmod +x vault
          sudo mv vault /usr/local/bin/vault

      - name: Connect to Central Argo CD cluster
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/.kube"

          export VAULT_ADDR="${{ secrets.VAULT_ADDR_PROD }}"
          export VAULT_TOKEN="${{ secrets.VAULT_TOKEN_PROD }}"

          vault kv get \
            -field=config \
            "$CENTRAL_ARGOCD_VAULT_PATH" \
            > "$HOME/.kube/config"

          chmod 600 "$HOME/.kube/config"

          CONTEXT="$(yq e '.current-context' "$HOME/.kube/config")"

          if [[ -z "$CONTEXT" || "$CONTEXT" == "null" ]]; then
            echo "Central Argo CD context was not found"
            exit 1
          fi

          kubectl --context="$CONTEXT" cluster-info
          echo "ARGOCD_KUBE_CONTEXT=$CONTEXT" >> "$GITHUB_ENV"

      - name: Generate Staging Application
        env:
          TEMPLATE_FILE: ${{ needs.resolve-deployment.outputs.template_file }}
          APPLICATION_NAME: ${{ needs.resolve-deployment.outputs.application_name }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          GENERATED="/tmp/sde-staging-app.yaml"

          cp "$TEMPLATE_FILE" "$GENERATED"

          sed -i "s|REPLACE_APPLICATION|${APPLICATION_NAME}|g" "$GENERATED"
          sed -i "s|REPLACE_BACKEND_TAG|${BACKEND_TAG}|g" "$GENERATED"
          sed -i "s|REPLACE_FRONTEND_TAG|${FRONTEND_TAG}|g" "$GENERATED"

          if grep -q 'REPLACE_' "$GENERATED"; then
            echo "Unresolved placeholders:"
            grep 'REPLACE_' "$GENERATED"
            exit 1
          fi

      - name: Apply Staging Application
        shell: bash
        run: |
          set -euo pipefail

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            apply \
            -f /tmp/sde-staging-app.yaml

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            get application sde-application-staging \
            -n argocd

  # =================================================================
  # Production — arenax-prod.
  # =================================================================
  deploy-production:
    name: Deploy Production
    needs:
      - resolve-deployment
      - update-image-tag

    if: |
      needs.resolve-deployment.outputs.deployment_type == 'production'

    runs-on: ubuntu-latest
    environment: sde-production

    steps:
      - name: Checkout release branch
        uses: actions/checkout@v4
        with:
          ref: ${{ needs.resolve-deployment.outputs.target_branch }}
          fetch-depth: 0

      - name: Install tools
        shell: bash
        run: |
          set -euo pipefail

          KUBECTL_VERSION="$(curl -L -s https://dl.k8s.io/release/stable.txt)"

          curl -LO -s \
            "https://dl.k8s.io/release/${KUBECTL_VERSION}/bin/linux/amd64/kubectl"

          chmod +x kubectl
          sudo mv kubectl /usr/local/bin/kubectl

          sudo wget -qO /usr/local/bin/yq \
            https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64

          sudo chmod +x /usr/local/bin/yq

          curl -sSL \
            "https://releases.hashicorp.com/vault/${VAULT_VERSION}/vault_${VAULT_VERSION}_linux_amd64.zip" \
            -o vault.zip

          unzip -q vault.zip
          chmod +x vault
          sudo mv vault /usr/local/bin/vault

      - name: Connect to Central Argo CD cluster
        shell: bash
        run: |
          set -euo pipefail

          mkdir -p "$HOME/.kube"

          export VAULT_ADDR="${{ secrets.VAULT_ADDR_PROD }}"
          export VAULT_TOKEN="${{ secrets.VAULT_TOKEN_PROD }}"

          vault kv get \
            -field=config \
            "$CENTRAL_ARGOCD_VAULT_PATH" \
            > "$HOME/.kube/config"

          chmod 600 "$HOME/.kube/config"

          CONTEXT="$(yq e '.current-context' "$HOME/.kube/config")"

          if [[ -z "$CONTEXT" || "$CONTEXT" == "null" ]]; then
            echo "Central Argo CD context was not found"
            exit 1
          fi

          kubectl --context="$CONTEXT" cluster-info
          echo "ARGOCD_KUBE_CONTEXT=$CONTEXT" >> "$GITHUB_ENV"

      - name: Generate Production Application
        env:
          TEMPLATE_FILE: ${{ needs.resolve-deployment.outputs.template_file }}
          APPLICATION_NAME: ${{ needs.resolve-deployment.outputs.application_name }}
          TARGET_BRANCH: ${{ needs.resolve-deployment.outputs.target_branch }}
          BACKEND_TAG: ${{ needs.resolve-deployment.outputs.backend_tag }}
          FRONTEND_TAG: ${{ needs.resolve-deployment.outputs.frontend_tag }}

        shell: bash
        run: |
          set -euo pipefail

          GENERATED="/tmp/sde-prod-app.yaml"

          cp "$TEMPLATE_FILE" "$GENERATED"

          sed -i "s|REPLACE_APPLICATION|${APPLICATION_NAME}|g" "$GENERATED"
          sed -i "s|REPLACE_BRANCH|${TARGET_BRANCH}|g" "$GENERATED"
          sed -i "s|REPLACE_BACKEND_TAG|${BACKEND_TAG}|g" "$GENERATED"
          sed -i "s|REPLACE_FRONTEND_TAG|${FRONTEND_TAG}|g" "$GENERATED"

          if grep -q 'REPLACE_' "$GENERATED"; then
            echo "Unresolved placeholders:"
            grep 'REPLACE_' "$GENERATED"
            exit 1
          fi

      - name: Apply Production Application
        shell: bash
        run: |
          set -euo pipefail

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            apply \
            -f /tmp/sde-prod-app.yaml

          kubectl \
            --context="$ARGOCD_KUBE_CONTEXT" \
            get application sde-application-prod \
            -n argocd

  # =================================================================
  # Release the Helm chart only after production deployment.
  # =================================================================
  chart-release:
    name: Chart Release
    needs: deploy-production

    if: |
      github.event_name == 'workflow_dispatch' &&
      github.event.inputs.environment == 'prod' &&
      startsWith(github.ref_name, 'release/')

    uses: ./.github/workflows/chart-release.yml
    secrets: inherit